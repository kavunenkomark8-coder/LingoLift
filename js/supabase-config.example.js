/**
 * Example Supabase client credentials.
 *
 * 1. Edit `supabase-config.js` in this folder (`FALLBACK_*`) or copy this file over it.
 * 2. Or on static hosting: set `globalThis.__LINGOLIFT_SUPABASE__ = { url, anonKey }` in a script before `app.js` (see README).
 * 3. In Supabase Dashboard → Project Settings → API, copy Project URL and anon public key.
 * 4. Enable Authentication → Providers → Anonymous sign-ins before using the app.
 */
export const SUPABASE_URL = 'https://YOUR_PROJECT_REF.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
