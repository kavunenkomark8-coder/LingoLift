/**
 * Supabase client credentials (replace placeholders before cloud sync).
 *
 * Copy from `supabase-config.example.js` if you prefer; this file is tracked so
 * the app and service worker always resolve the module. Use your Project URL
 * and anon public key from Dashboard → Project Settings → API.
 *
 * Enable Authentication → Providers → Anonymous before using the app.
 */
export const SUPABASE_URL = 'https://YOUR_PROJECT_REF.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
